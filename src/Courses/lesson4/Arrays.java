package Courses.lesson4;

import java.io.File;

public class Arrays {
    public static void main(String[] args) {
        /* String[] array = new String[4]; //создание массива строк из 4 элементов
        //индекс - номер ячейки
        array[0] = "Hello";
        array[1] = "kurwa";
        array[2] = "ja";
        array[3] = "pierdolem";
        //System.out.println(array[1]); //обращение к индексу ячейки
        /* for (int i = 0; i < 4; i++) {
            System.out.println(array[i]);//выведет нулевую ячейку, в следущей с индексом 1 и далее */
            /* for (int i = 0; i < array.length; i++) {  //считает сразу длину массива (этот прием гораздо лучше)
                System.out.println(array[i]); */

        /* Scanner scanner = new Scanner(System.in);
        double[] masDouble = new double[scanner.nextInt()];
        for (int i = 0; i < masDouble.length; i++) {
            if (i % 2 != 0){
                masDouble[i] = Math.pow(masDouble[i],2);
            }
            System.out.println(masDouble[i]);

        }  */

    }
}
